language: node_js
node_js:
  - "18"
cache:
  directories:
    - node_modules
script:
  - npm ci
  - npm run build
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  local_dir: public
  target-branch: gh-pages
  keep-history: true
  on:
    branch: main
